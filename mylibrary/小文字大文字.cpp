#include <bits/stdc++.h>
using namespace std;

int main(){
    char c;
    char c = 'A';

    char lower = tolower(c); // 'a'
    char upper = toupper(c); // 'A'
    
    if (islower(c)) {
        // c は小文字
    }

    if (isupper(c)) {
        // c は大文字
    }
}